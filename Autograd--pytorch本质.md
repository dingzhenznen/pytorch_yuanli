正向计算loss,forward()计算梯度 （激活函数的梯度 和 因此层作为参数计算后 求导的时候输入参会会影响梯度需要layernorm）
深度学习的训练本质上是一个反复求梯度、更新参数的过程。

手动推导每一层的梯度既繁琐又容易出错，PyTorch 的 Autograd（自动微分）引擎正是为了解决这个问题而生——它能够自动计算任意计算图的梯度，让你专注于模型设计，而不是微积分推导。

optim 模块 简化了更新参数