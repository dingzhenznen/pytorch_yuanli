看到 <bos>          预测 i
  看到 <bos> i        预测 love
  看到 <bos> i love   预测 you
  看到 <bos> i love you 预测 <eos>

  然后分别计算 loss：

  预测 i      vs 真实 i
  预测 love   vs 真实 love
  预测 you    vs 真实 you
  预测 <eos>  vs 真实 <eos>


训练时：并行预测每个词，用真实前文，不用模型自己刚预测的词。

推理时：一个词一个词生成，用模型上一步预测出来的词继续预测下一个词



logits = self.lm_head(hidden_states[:, slice_indices, :]) 并行训练时 会得到 预测每个词的 概率是吗


• 差不多，但这里得到的首先是 logits，不是概率。

  logits = self.lm_head(hidden_states[:, slice_indices, :])

  含义是：

  hidden_states: (batch_size, seq_len, hidden_size)
  lm_head 后:    (batch_size, seq_len, vocab_size)

  也就是说，模型会对每个位置都输出一个长度为 vocab_size 的向量。